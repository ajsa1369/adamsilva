# Vercel-compatible redirects for API routes
# Vercel serves serverless functions from `/api/*`, map directly
/api/contact /api/contact 200

# SPA fallback (kept for static hosts; Vercel uses `vercel.json`)
/* /index.html 200