---
phase: 09-package-pages-marketing-site
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified: []
autonomous: false
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - PKG-05

must_haves:
  truths:
    - "Prospect can arrive at /packages and compare all tiers side-by-side without scrolling past the fold on desktop"
    - "Prospect can navigate to /packages/gold and see pricing, included features, upgrade comparison, and CTA — then click 'Get Your Custom Proposal' and land on /get-started"
    - "Prospect can select 'Shopify' on /platform-check and immediately see why UCP/ACP/AP2 are blocked — without a page reload"
    - "ROI calculator on /packages shows a non-zero monthly revenue lift when: package=gold, leads=200, close rate=15%, deal size=$5,000"
    - "All 6 /packages/[tier] slugs respond with 200 (not 404)"
    - "JSON-LD is present in page source on /packages, /packages/bronze, /packages/gold, /platform-check"
  artifacts:
    - path: "app/(marketing)/packages/page.tsx"
      provides: "/packages index page — rendered and serving"
    - path: "app/(marketing)/packages/[tier]/page.tsx"
      provides: "All 6 tier detail pages — rendered and serving"
    - path: "app/(marketing)/platform-check/page.tsx"
      provides: "/platform-check interactive page — rendered and serving"
    - path: "components/ROICalculator.tsx"
      provides: "ROI calculator embedded in /packages"
    - path: "components/PlatformCheckTool.tsx"
      provides: "Client-side platform checker tool"
    - path: "lib/data/packages.ts"
      provides: "PACKAGES + PLATFORM_MATRIX static data"
  key_links:
    - from: "All /packages pages"
      to: "/get-started"
      via: "CTA buttons"
      pattern: "href.*get-started"
    - from: "/platform-check"
      to: "/tools/protocol-checker"
      via: "cross-tool link"
      pattern: "tools/protocol-checker"
---

<objective>
Human verification of all Phase 9 deliverables — confirm the self-service sales funnel works end-to-end in a browser before Phase 9 is marked complete.

Purpose: Phase 9's goal is "A prospect can understand pricing, check platform compatibility, calculate ROI, and reach the intake agent — all without talking to a human." This checkpoint verifies that the full prospect journey works as intended: /packages → tier detail → /get-started, and /platform-check → compliance result → /get-started.

Output: Human approval that all Phase 9 pages render correctly, interactive features work, and the CTA journey reaches /get-started.
</objective>

<execution_context>
@C:/Users/Bev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-package-pages-marketing-site/09-01-SUMMARY.md
@.planning/phases/09-package-pages-marketing-site/09-02-SUMMARY.md
@.planning/phases/09-package-pages-marketing-site/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start local dev server and run automated smoke checks</name>
  <files></files>
  <action>
Run the Next.js dev server and perform automated checks on all Phase 9 routes.

Start dev server in background (or use existing if running):
```bash
cd "C:/Users/Bev/Downloads/ASCW/adamsilva" && npm run dev &
```
Wait 10 seconds for server to start, then run:

```bash
# Check all Phase 9 routes return 200
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages/bronze
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages/silver
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages/gold
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages/core
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages/shopify-starter
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages/shopify-growth
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/platform-check

# Check JSON-LD is present in page source
curl -s http://localhost:3000/packages | grep -c "application/ld+json"
curl -s http://localhost:3000/packages/gold | grep -c "application/ld+json"
curl -s http://localhost:3000/platform-check | grep -c "application/ld+json"
```

Expected: All routes return 200. Each page source contains at least 1 `application/ld+json` script tag.

If any route returns 404 or 500 — fix the issue in the relevant file before proceeding to the checkpoint.

Also verify the /packages/unknown-tier returns 404 (notFound() working):
```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages/unknown-tier
```
Expected: 404.
  </action>
  <verify>
    <automated>cd "C:/Users/Bev/Downloads/ASCW/adamsilva" && curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/packages 2>/dev/null || echo "server not running — start with: npm run dev"</automated>
  </verify>
  <done>
    - All 8 Phase 9 routes return 200
    - /packages/unknown-tier returns 404
    - Each checked page contains at least 1 application/ld+json script tag in source
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 9 complete: 6 new files built across the marketing conversion layer.

1. `lib/data/packages.ts` — PACKAGES (6 tiers with PRD pricing) + PLATFORM_MATRIX (8 platforms with compliance matrix)
2. `components/ROICalculator.tsx` — Client-side ROI calculator (4 inputs, 3 computed outputs, no API calls)
3. `components/PlatformCheckTool.tsx` — Client-side platform selector with compliance matrix and CTA routing
4. `app/(marketing)/packages/page.tsx` — Side-by-side tier comparison with PricingTable, ComparisonTable, ROI calculator, sticky CTA
5. `app/(marketing)/packages/[tier]/page.tsx` — Individual tier pages with generateStaticParams (all 6 slugs), upgrade nudge, PlatformWarning for legacy tiers, FAQ, CTA to /get-started
6. `app/(marketing)/platform-check/page.tsx` — Platform compatibility checker with JSON-LD, links to PlatformCheckTool

All pages have JSON-LD (ItemList+FAQPage on /packages; Service+FAQPage+BreadcrumbList on /packages/[tier]; SoftwareApplication+FAQPage+BreadcrumbList on /platform-check).
  </what-built>
  <how-to-verify>
Open http://localhost:3000 (or your Vercel preview URL if deployed) and test these flows:

**Flow 1 — Packages comparison page:**
1. Navigate to /packages
2. Confirm: hero headline "Agentic Commerce Packages" visible
3. Confirm: 4 tier cards visible (Bronze $16,000, Silver $28,000, Gold $48,000, Core)
4. Confirm: ComparisonTable below with blog posts, chatbot channels, protocol stack rows
5. Confirm: ROI calculator section visible — change leads slider/input, confirm monthly lift number updates
6. Confirm: "Shopify Starter →" and "Shopify Growth →" links visible in callout
7. Confirm: On mobile (375px): sticky "Get Your Custom Proposal" bar at bottom visible

**Flow 2 — Individual tier detail (Gold):**
1. Click "View Gold →" link on /packages OR navigate to /packages/gold
2. Confirm: "$48,000" setup price visible in hero
3. Confirm: "What's Included" section shows "Web + SMS + Voice + WhatsApp" chatbot channels
4. Confirm: ComparisonTable vs Core (upgrade nudge) visible
5. Confirm: CTA button "Start with Gold — Get Your Custom Proposal" visible and clicking takes you to /get-started
6. Confirm: FAQ section with at least 3 questions visible

**Flow 3 — Shopify tier page:**
1. Navigate to /packages/shopify-starter
2. Confirm: PlatformWarning amber box visible ("Shopify Platform Detected")
3. Confirm: "Legacy Add-On Path" and "Migration Path" options visible in warning

**Flow 4 — Platform compliance checker:**
1. Navigate to /platform-check
2. Confirm: Platform dropdown visible
3. Select "Shopify" from dropdown
4. Confirm: Compliance matrix appears instantly (no page reload)
5. Confirm: UCP = ✗, ACP = ✗, AP2 = ✗, Gold Standard = ✗ in compliance matrix
6. Confirm: 5 legacy penalty items listed (Hydration Tax, Root-Level Restriction, etc.)
7. Confirm: PlatformWarning amber box visible
8. Confirm: "Shopify Starter" package recommendation card with "View Shopify Starter Package →" link
9. Select "Next.js / Custom SPA" — confirm all 7 rows show ✓ green checks
10. Confirm: "Check your domain's live protocol endpoints →" link present and pointing to /tools/protocol-checker

**Flow 5 — ROI calculation spot check:**
1. On /packages, find the ROI calculator
2. Set: Package=Gold, Leads/Month=200, Close Rate=15%, Deal Size=$5,000
3. Expected monthly revenue lift: 200 × 0.40 × 0.15 × $5,000 = $60,000
4. Confirm: Monthly Revenue Lift shows approximately $60,000

**Schema check:**
1. In browser, view source for /packages
2. Search for "application/ld+json" — confirm at least one script tag present
3. Within the JSON-LD, confirm "ItemList" type is present
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 flows pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 5 user flows in the checkpoint verified. JSON-LD present on all Phase 9 pages. Build passing.
</verification>

<success_criteria>
1. All 8 Phase 9 routes (1 packages index + 6 tier pages + 1 platform-check) return 200
2. ROI calculator on /packages returns correct math: Gold + 200 leads + 15% close + $5K deal = $60,000/mo lift
3. /platform-check Shopify selection shows UCP/ACP/AP2 all as ✗ with 5 penalty items
4. /platform-check Next.js/Custom shows all 7 capabilities as ✓
5. All /packages/[tier] CTAs link to /get-started
6. JSON-LD present (application/ld+json) on /packages, /packages/gold, /platform-check
7. Human approves all 5 verification flows
</success_criteria>

<output>
After completion, create `.planning/phases/09-package-pages-marketing-site/09-04-SUMMARY.md`

Then update `.planning/STATE.md`:
- Phase 9 status: Complete
- All 4 plans: 09-01 through 09-04 marked complete
- Add Phase 9 to completed phases
- Note: PKG-01 through PKG-05 all satisfied
- Next: Phase 10 (Vercel MCP Server & Protocol Stack)
</output>
