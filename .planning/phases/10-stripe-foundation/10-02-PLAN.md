---
phase: 10-stripe-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - lib/stripe/products.ts
autonomous: false
requirements:
  - STRIPE-02
  - STRIPE-03

user_setup:
  - service: stripe
    why: "Products and Prices must be created in Stripe Dashboard (test mode) before env vars can be set"
    env_vars:
      - name: STRIPE_PRODUCT_BRONZE
        source: "Stripe Dashboard > Products > ASC Bronze Package > Product ID (prod_...)"
      - name: STRIPE_PRICE_BRONZE_SETUP
        source: "Stripe Dashboard > Products > ASC Bronze Package > One-time Price ID (price_...)"
      - name: STRIPE_PRICE_BRONZE_MONTHLY
        source: "Stripe Dashboard > Products > ASC Bronze Package > Recurring Price ID (price_...)"
      - name: STRIPE_PRODUCT_SILVER
        source: "Stripe Dashboard > Products > ASC Silver Package > Product ID"
      - name: STRIPE_PRICE_SILVER_SETUP
        source: "Stripe Dashboard > Products > ASC Silver Package > One-time Price ID"
      - name: STRIPE_PRICE_SILVER_MONTHLY
        source: "Stripe Dashboard > Products > ASC Silver Package > Recurring Price ID"
      - name: STRIPE_PRODUCT_GOLD
        source: "Stripe Dashboard > Products > ASC Gold Package > Product ID"
      - name: STRIPE_PRICE_GOLD_SETUP
        source: "Stripe Dashboard > Products > ASC Gold Package > One-time Price ID"
      - name: STRIPE_PRICE_GOLD_MONTHLY
        source: "Stripe Dashboard > Products > ASC Gold Package > Recurring Price ID"
      - name: STRIPE_PRODUCT_SHOPIFY_STARTER
        source: "Stripe Dashboard > Products > ASC Shopify Starter Package > Product ID"
      - name: STRIPE_PRICE_SHOPIFY_STARTER_SETUP
        source: "Stripe Dashboard > Products > ASC Shopify Starter Package > One-time Price ID"
      - name: STRIPE_PRICE_SHOPIFY_STARTER_MONTHLY
        source: "Stripe Dashboard > Products > ASC Shopify Starter Package > Recurring Price ID"
      - name: STRIPE_PRODUCT_SHOPIFY_GROWTH
        source: "Stripe Dashboard > Products > ASC Shopify Growth Package > Product ID"
      - name: STRIPE_PRICE_SHOPIFY_GROWTH_SETUP
        source: "Stripe Dashboard > Products > ASC Shopify Growth Package > One-time Price ID"
      - name: STRIPE_PRICE_SHOPIFY_GROWTH_MONTHLY
        source: "Stripe Dashboard > Products > ASC Shopify Growth Package > Recurring Price ID"
    dashboard_config:
      - task: "Create 5 Products (Bronze, Silver, Gold, Shopify Starter, Shopify Growth) in TEST mode"
        location: "Stripe Dashboard > Products (https://dashboard.stripe.com/test/products)"
      - task: "Create 2 Prices per Product (one-time setup + recurring monthly)"
        location: "Each Product page > Add price"

must_haves:
  truths:
    - "Every fixed-price package slug maps to a Stripe Product ID and two Price IDs (setup + monthly)"
    - "Core tier explicitly returns null (custom-quoted, no Stripe Price)"
    - "getStripePricing('bronze') returns a StripeProductPricing object with all three IDs"
    - "getStripePricing('core') returns null"
    - "Missing any env var for a fixed-price tier throws at import time"
  artifacts:
    - path: "lib/stripe/products.ts"
      provides: "Typed map from package slugs to Stripe Product/Price IDs"
      exports: ["STRIPE_PRODUCTS", "getStripePricing"]
  key_links:
    - from: "lib/stripe/products.ts"
      to: "lib/stripe/types.ts"
      via: "imports StripeProductPricing, StripePriceMap, PackageSlug"
      pattern: "import.*StripeProductPricing.*from.*types"
    - from: "lib/stripe/products.ts"
      to: "process.env.STRIPE_PRODUCT_*"
      via: "getEnv() at module level for all 15 env vars"
      pattern: "getEnv.*STRIPE_PRODUCT"
---

<objective>
Create the typed Product/Price ID map that connects ASC package slugs to their Stripe Products and Prices, and provide a step-by-step Dashboard creation guide so the user can populate the env vars.

Purpose: Phases 12 and 13 need to look up Stripe Price IDs by package slug to create PaymentIntents and Checkout Sessions. This file is the single source of truth for that mapping.

Output: `lib/stripe/products.ts` with the typed map and helper function, plus a checkpoint for the user to create Products/Prices in Stripe Dashboard.
</objective>

<execution_context>
@C:/Users/Bev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-stripe-foundation/10-RESEARCH.md
@.planning/phases/10-stripe-foundation/10-01-SUMMARY.md

@lib/data/packages.ts (canonical package data with pricing amounts)

<interfaces>
<!-- From Plan 10-01 outputs -->

From lib/stripe/types.ts (created in 10-01):
```typescript
import type { PackagePageData } from '@/lib/data/packages'

export type PackageSlug = PackagePageData['slug']

export interface StripeProductPricing {
  productId: string
  setupPriceId: string
  monthlyPriceId: string
}

export type StripePriceMap = {
  [K in Exclude<PackageSlug, 'core'>]: StripeProductPricing
} & {
  core: null
}
```

Package pricing from lib/data/packages.ts:
```
Bronze:          setup $16,000  | monthly $3,500
Silver:          setup $28,000  | monthly $6,500
Gold:            setup $48,000  | monthly $12,000
Shopify Starter: setup $8,500   | monthly $2,000
Shopify Growth:  setup $16,000  | monthly $4,000
Core:            custom-quoted  | custom-quoted (no Stripe Price)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed Stripe Products/Prices map</name>
  <files>lib/stripe/products.ts</files>
  <action>
Create `lib/stripe/products.ts` that:

1. Imports `StripeProductPricing`, `StripePriceMap`, and `PackageSlug` from `@/lib/stripe/types`

2. Creates a `getEnv()` helper (same pattern as client.ts) that throws with a descriptive message if an env var is missing

3. Defines `STRIPE_PRODUCTS` as a `StripePriceMap` object:
   - `bronze`: reads `STRIPE_PRODUCT_BRONZE`, `STRIPE_PRICE_BRONZE_SETUP`, `STRIPE_PRICE_BRONZE_MONTHLY`
   - `silver`: reads `STRIPE_PRODUCT_SILVER`, `STRIPE_PRICE_SILVER_SETUP`, `STRIPE_PRICE_SILVER_MONTHLY`
   - `gold`: reads `STRIPE_PRODUCT_GOLD`, `STRIPE_PRICE_GOLD_SETUP`, `STRIPE_PRICE_GOLD_MONTHLY`
   - `shopify-starter`: reads `STRIPE_PRODUCT_SHOPIFY_STARTER`, `STRIPE_PRICE_SHOPIFY_STARTER_SETUP`, `STRIPE_PRICE_SHOPIFY_STARTER_MONTHLY`
   - `shopify-growth`: reads `STRIPE_PRODUCT_SHOPIFY_GROWTH`, `STRIPE_PRICE_SHOPIFY_GROWTH_SETUP`, `STRIPE_PRICE_SHOPIFY_GROWTH_MONTHLY`
   - `core`: `null` (custom-quoted, no fixed Stripe Price)

4. All env var reads happen at module level (top of file), NOT inside functions — this ensures missing vars are caught at import time, not at checkout time

5. Exports a helper function:
   ```typescript
   export function getStripePricing(slug: PackageSlug): StripeProductPricing | null {
     return STRIPE_PRODUCTS[slug]
   }
   ```

6. Add a comment block at the top with the pricing reference table:
   ```
   // Package tier -> Stripe pricing mapping
   // Bronze:          setup $16,000  | monthly $3,500
   // Silver:          setup $28,000  | monthly $6,500
   // Gold:            setup $48,000  | monthly $12,000
   // Shopify Starter: setup $8,500   | monthly $2,000
   // Shopify Growth:  setup $16,000  | monthly $4,000
   // Core:            custom-quoted  (no Stripe Price -- null)
   //
   // Products and Prices are created in Stripe Dashboard (test mode).
   // Copy IDs to .env.local. See RESEARCH.md "Dashboard Product Creation Guide".
   ```

Do NOT create Products via Stripe API — they are created manually in Dashboard per the research recommendation for a 5-product catalog.
  </action>
  <verify>
    <automated>cd C:/Users/Bev/Downloads/ASCW/adamsilva && npx tsc --noEmit lib/stripe/products.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    - `lib/stripe/products.ts` exports STRIPE_PRODUCTS and getStripePricing
    - All 5 fixed-price tiers read from env vars at module level
    - Core tier is explicitly null
    - Type checking passes (StripePriceMap type enforces all slugs are present)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Stripe Dashboard Products and Prices created</name>
  <action>
User creates 5 Stripe Products with 10 Prices in test mode and populates 15 env vars in .env.local. This is a Dashboard-only operation that Claude cannot automate.
  </action>
  <what-built>
Typed Stripe Products/Prices map at `lib/stripe/products.ts` that reads 15 env vars (3 per tier x 5 tiers) to map package slugs to Stripe Product and Price IDs. The code is ready — but the env vars need to be populated from Stripe Dashboard.
  </what-built>
  <how-to-verify>
**You need to create Products and Prices in Stripe Dashboard (test mode).** Here are the steps:

1. Go to https://dashboard.stripe.com/test/products
2. For each tier, click "+ Add product" and create:

   **Bronze Package:**
   - Name: "ASC Bronze Package"
   - Add one-time price: $16,000.00 (setup fee)
   - Add recurring price: $3,500.00/month (retainer)
   - Copy: Product ID, both Price IDs

   **Silver Package:**
   - Name: "ASC Silver Package"
   - Add one-time price: $28,000.00 (setup fee)
   - Add recurring price: $6,500.00/month (retainer)

   **Gold Package:**
   - Name: "ASC Gold Package"
   - Add one-time price: $48,000.00 (setup fee)
   - Add recurring price: $12,000.00/month (retainer)

   **Shopify Starter Package:**
   - Name: "ASC Shopify Starter Package"
   - Add one-time price: $8,500.00 (setup fee)
   - Add recurring price: $2,000.00/month (retainer)

   **Shopify Growth Package:**
   - Name: "ASC Shopify Growth Package"
   - Add one-time price: $16,000.00 (setup fee)
   - Add recurring price: $4,000.00/month (retainer)

3. Add all 15 IDs to `.env.local`:
   ```
   STRIPE_PRODUCT_BRONZE=prod_...
   STRIPE_PRICE_BRONZE_SETUP=price_...
   STRIPE_PRICE_BRONZE_MONTHLY=price_...
   STRIPE_PRODUCT_SILVER=prod_...
   STRIPE_PRICE_SILVER_SETUP=price_...
   STRIPE_PRICE_SILVER_MONTHLY=price_...
   STRIPE_PRODUCT_GOLD=prod_...
   STRIPE_PRICE_GOLD_SETUP=price_...
   STRIPE_PRICE_GOLD_MONTHLY=price_...
   STRIPE_PRODUCT_SHOPIFY_STARTER=prod_...
   STRIPE_PRICE_SHOPIFY_STARTER_SETUP=price_...
   STRIPE_PRICE_SHOPIFY_STARTER_MONTHLY=price_...
   STRIPE_PRODUCT_SHOPIFY_GROWTH=prod_...
   STRIPE_PRICE_SHOPIFY_GROWTH_SETUP=price_...
   STRIPE_PRICE_SHOPIFY_GROWTH_MONTHLY=price_...
   ```

4. **Skip Core tier** — it is custom-quoted and has no fixed Stripe Price.

**Total:** 5 Products, 10 Prices, 15 env vars.
  </how-to-verify>
  <verify>User confirms all 15 env vars are set in .env.local</verify>
  <done>All 5 Stripe Products and 10 Prices exist in test mode; 15 env vars populated in .env.local</done>
  <resume-signal>Type "products created" once all 5 Products and 10 Prices exist in Stripe test mode and env vars are in .env.local, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit lib/stripe/products.ts` passes
- `getStripePricing('bronze')` returns an object with productId, setupPriceId, monthlyPriceId
- `getStripePricing('core')` returns null
- All 15 env vars are documented and needed before any downstream plan uses this file
- `npm run build` still passes (products.ts is not imported by any page route yet)
</verification>

<success_criteria>
- lib/stripe/products.ts created with typed map for all 6 slugs
- 5 fixed-price tiers read from env vars at module level
- Core tier is null (custom-quoted)
- getStripePricing helper exported for downstream use
- TypeScript type checking passes
- User has been guided through Stripe Dashboard Product/Price creation
</success_criteria>

<output>
After completion, create `.planning/phases/10-stripe-foundation/10-02-SUMMARY.md`
</output>
