---
phase: 02-supabase-schema-data-architecture
plan: 06
type: execute
wave: 2
depends_on: [02-04]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "generate-proposal edge function is deployed and returns HTTP 200 with JSON body"
    - "send-proposal-email edge function is deployed and returns HTTP 200 with JSON body"
    - "create-crm-deal edge function is deployed and returns HTTP 200 with JSON body"
    - "All 3 functions are visible in Supabase dashboard under Edge Functions"
  artifacts:
    - path: "supabase/functions/generate-proposal/index.ts"
      provides: "Deployed to Supabase — callable at functions URL"
      contains: "generate-proposal scaffold"
    - path: "supabase/functions/send-proposal-email/index.ts"
      provides: "Deployed to Supabase — callable at functions URL"
      contains: "send-proposal-email scaffold"
    - path: "supabase/functions/create-crm-deal/index.ts"
      provides: "Deployed to Supabase — callable at functions URL"
      contains: "create-crm-deal scaffold"
  key_links:
    - from: "https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/generate-proposal"
      to: "Phase 4 intake agent"
      via: "Phase 4 replaces the scaffold body with full proposal generation logic"
      pattern: "supabase.functions.invoke('generate-proposal', { body: proposalData })"
---

<objective>
Deploy the three edge function scaffolds (generate-proposal, send-proposal-email, create-crm-deal) to the live Supabase project using the Supabase CLI. Verify each function returns HTTP 200 when invoked. Includes a human-verify checkpoint to confirm the functions appear in the Supabase dashboard.

Purpose: Edge functions must be deployed (not just written) for Phase 2 to be complete. Phase 4 will invoke these functions by name — they must exist as live endpoints before Phase 4 begins implementation.

Output: 3 live edge function endpoints under https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/
</objective>

<execution_context>
@C:/Users/Bev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-supabase-schema-data-architecture/02-CONTEXT.md

<interfaces>
<!-- Supabase CLI deploy commands -->
-- Deploy a single function:
npx supabase functions deploy generate-proposal --project-ref cdifobufvgfpbcbvicjs

-- Deploy all functions:
npx supabase functions deploy --project-ref cdifobufvgfpbcbvicjs

-- Invoke a deployed function (with anon key from .env.local):
curl -X POST https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/generate-proposal \
  -H "Authorization: Bearer ANON_KEY" \
  -H "Content-Type: application/json" \
  -d '{"test": true}'

-- Function URLs:
https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/generate-proposal
https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/send-proposal-email
https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/create-crm-deal

-- SUPABASE_ANON_KEY: read from .env.local file at project root
-- Access token for CLI: sbp_9763ae3189e29a7f5180816da338c4058e192d2a
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy all three edge function scaffolds to Supabase</name>
  <files></files>
  <action>
From project root C:/Users/Bev/Downloads/ASCW/adamsilva:

STEP 1 — Authenticate Supabase CLI if needed:
  npx supabase login
  (Access token: sbp_9763ae3189e29a7f5180816da338c4058e192d2a)
  OR set env var: export SUPABASE_ACCESS_TOKEN=sbp_9763ae3189e29a7f5180816da338c4058e192d2a

STEP 2 — Deploy each scaffold function individually (more reliable than deploy --all):
  npx supabase functions deploy generate-proposal --project-ref cdifobufvgfpbcbvicjs
  npx supabase functions deploy send-proposal-email --project-ref cdifobufvgfpbcbvicjs
  npx supabase functions deploy create-crm-deal --project-ref cdifobufvgfpbcbvicjs

  Each deploy should output: "Deployed Function generate-proposal on project cdifobufvgfpbcbvicjs"

STEP 3 — Read NEXT_PUBLIC_SUPABASE_ANON_KEY from .env.local:
  grep NEXT_PUBLIC_SUPABASE_ANON_KEY C:/Users/Bev/Downloads/ASCW/adamsilva/.env.local

  NOTE: If .env.local does not exist or ANON_KEY is empty, use the Supabase dashboard to find it at:
  https://supabase.com/dashboard/project/cdifobufvgfpbcbvicjs/settings/api

STEP 4 — Test each function with curl:
  ANON_KEY=$(grep NEXT_PUBLIC_SUPABASE_ANON_KEY C:/Users/Bev/Downloads/ASCW/adamsilva/.env.local | cut -d= -f2)

  curl -s -X POST https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/generate-proposal \
    -H "Authorization: Bearer $ANON_KEY" \
    -H "Content-Type: application/json" \
    -d '{"test": true}'

  curl -s -X POST https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/send-proposal-email \
    -H "Authorization: Bearer $ANON_KEY" \
    -H "Content-Type: application/json" \
    -d '{"test": true}'

  curl -s -X POST https://cdifobufvgfpbcbvicjs.supabase.co/functions/v1/create-crm-deal \
    -H "Authorization: Bearer $ANON_KEY" \
    -H "Content-Type: application/json" \
    -d '{"test": true}'

Expected for each: HTTP 200, body = {"success":true,"message":"[function-name] scaffold","phase":4}

FALLBACK — If ANON_KEY is not set in .env.local yet:
  Use the service role key from the dashboard for testing (it also bypasses RLS).
  Do not commit the service role key anywhere.

If any function returns an error other than 401 (auth) or 404:
  - Check function file exists: ls supabase/functions/[name]/index.ts
  - Re-deploy: npx supabase functions deploy [name] --project-ref cdifobufvgfpbcbvicjs
  - Check logs: npx supabase functions logs [name] --project-ref cdifobufvgfpbcbvicjs
  </action>
  <verify>
    <automated>cd C:/Users/Bev/Downloads/ASCW/adamsilva &amp;&amp; npx supabase functions list --project-ref cdifobufvgfpbcbvicjs 2>&amp;1</automated>
  </verify>
  <done>npx supabase functions list shows generate-proposal, send-proposal-email, and create-crm-deal in the active functions list. Each curl test returned HTTP 200 with the scaffold JSON body.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Deployed 3 edge function scaffolds to Supabase project cdifobufvgfpbcbvicjs:
  - generate-proposal: returns {"success":true,"message":"generate-proposal scaffold","phase":4}
  - send-proposal-email: returns {"success":true,"message":"send-proposal-email scaffold","phase":4}
  - create-crm-deal: returns {"success":true,"message":"create-crm-deal scaffold","phase":4}
  </what-built>
  <how-to-verify>
  1. Go to: https://supabase.com/dashboard/project/cdifobufvgfpbcbvicjs/functions
  2. Confirm all 3 new functions appear: generate-proposal, send-proposal-email, create-crm-deal
  3. Optionally click "generate-proposal" and run a test invocation from the dashboard to confirm 200 response
  4. Also verify the existing 3 functions (email-service, generate-guide, team-notifications) are still present and unaffected
  </how-to-verify>
  <resume-signal>Type "verified" once all 3 functions appear in the dashboard and return non-error responses. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 3 edge functions deployed and accessible.
Run: npx supabase functions list --project-ref cdifobufvgfpbcbvicjs — shows generate-proposal, send-proposal-email, create-crm-deal
Curl test each function URL with anon key — each returns HTTP 200 and scaffold JSON.
</verification>

<success_criteria>
- All 3 functions visible in Supabase dashboard under Edge Functions
- Each function returns HTTP 200 with {"success":true,"message":"[name] scaffold","phase":4}
- Existing functions (email-service, generate-guide, team-notifications) are unaffected
- Human confirms via dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-schema-data-architecture/02-06-SUMMARY.md`
</output>
