---
phase: 02-supabase-schema-data-architecture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/011_seed_integrations_catalog.sql
  - supabase/migrations/012_seed_packages.sql
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Querying integrations_catalog by name='HubSpot' returns tier=1, setup_cost=750, monthly_cost=150"
    - "Querying integrations_catalog by name='NetSuite' returns tier=3, setup_cost=4000, monthly_cost=500"
    - "Querying packages by slug='gold' returns tier1_slots=12, tier2_slots=3, tier3_slots=1"
    - "Querying packages by slug='core' returns tier1_slots=99 (unlimited)"
    - "All 6 package slugs exist: bronze, silver, gold, core, shopify-starter, shopify-growth"
  artifacts:
    - path: "supabase/migrations/011_seed_integrations_catalog.sql"
      provides: "40+ integration tool rows across all tiers and categories"
      contains: "INSERT INTO integrations_catalog"
    - path: "supabase/migrations/012_seed_packages.sql"
      provides: "6 package rows: Bronze, Silver, Gold, Core, Shopify Starter, Shopify Growth"
      contains: "INSERT INTO packages"
  key_links:
    - from: "supabase/migrations/011_seed_integrations_catalog.sql"
      to: "supabase/migrations/004_integrations_catalog.sql"
      via: "INSERT INTO integrations_catalog — table must exist first (migration 004)"
      pattern: "INSERT INTO integrations_catalog"
    - from: "supabase/migrations/012_seed_packages.sql"
      to: "supabase/migrations/005_packages.sql"
      via: "INSERT INTO packages — table must exist first (migration 005)"
      pattern: "INSERT INTO packages"
---

<objective>
Write two SQL seed migration files: one that populates integrations_catalog with all known tools from the intake flow (40+ rows across Tier 1/2/3), and one that seeds the packages table with all 6 package definitions.

Purpose: The pricing engine (Phase 3) and intake agent (Phase 4) look up tools by name and retrieve tier classification and costs. Without seed data, catalog lookups return nothing and proposals cannot be generated. This seed must cover every tool mentioned in PRD Module 1.

Output: 2 idempotent SQL seed files using INSERT ... ON CONFLICT DO NOTHING to allow re-running without duplicating rows.
</objective>

<execution_context>
@C:/Users/Bev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-supabase-schema-data-architecture/02-CONTEXT.md

<interfaces>
<!-- integrations_catalog schema (from migration 004) -->
```sql
integrations_catalog (
  id UUID DEFAULT gen_random_uuid(),
  name TEXT NOT NULL UNIQUE,
  category TEXT NOT NULL,  -- crm, booking, email, payment, helpdesk, accounting, platform, erp
  tier INTEGER NOT NULL,   -- 1, 2, or 3
  setup_cost NUMERIC(10,2) NOT NULL,   -- T1=750, T2=1500, T3=4000
  monthly_cost NUMERIC(10,2) NOT NULL, -- T1=150, T2=250, T3=500
  api_available BOOLEAN NOT NULL DEFAULT true,
  connector_type TEXT,     -- 'pre-built', 'custom-rest', 'legacy', 'no-api'
  notes TEXT
)
```

<!-- packages schema (from migration 005) -->
```sql
packages (
  id UUID DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  slug TEXT NOT NULL UNIQUE,
  base_setup NUMERIC(10,2) NOT NULL,
  base_monthly NUMERIC(10,2) NOT NULL,
  tier1_slots INTEGER NOT NULL DEFAULT 0,
  tier2_slots INTEGER NOT NULL DEFAULT 0,
  tier3_slots INTEGER NOT NULL DEFAULT 0,
  features_json JSONB NOT NULL DEFAULT '[]',
  is_legacy_only BOOLEAN NOT NULL DEFAULT false
)
```

<!-- LOCKED package definitions from 02-CONTEXT.md -->
| Name           | Slug            | Setup | Monthly | T1  | T2  | T3  | Legacy |
|----------------|-----------------|-------|---------|-----|-----|-----|--------|
| Bronze         | bronze          | 16000 | 3500    | 3   | 0   | 0   | false  |
| Silver         | silver          | 28000 | 6500    | 6   | 1   | 0   | false  |
| Gold           | gold            | 48000 | 12000   | 12  | 3   | 1   | false  |
| Core           | core            | 75000 | 0       | 99  | 99  | 99  | false  |
| Shopify Starter| shopify-starter | 8500  | 2000    | 2   | 0   | 0   | true   |
| Shopify Growth | shopify-growth  | 16000 | 4000    | 4   | 1   | 0   | true   |

<!-- Tier classification rules from 02-CONTEXT.md -->
Tier 1: pre-built connector / well-documented REST API → setup_cost=750, monthly_cost=150
Tier 2: REST API requiring custom mapping or limited docs → setup_cost=1500, monthly_cost=250
Tier 3: legacy system, no public API, or custom build → setup_cost=4000, monthly_cost=500
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 011 — integrations_catalog seed (40+ tools)</name>
  <files>
    supabase/migrations/011_seed_integrations_catalog.sql
  </files>
  <action>
Write supabase/migrations/011_seed_integrations_catalog.sql using INSERT INTO integrations_catalog ... ON CONFLICT (name) DO NOTHING to make it idempotent.

Include ALL of the following tools (minimum — add others from PRD if mentioned):

CATEGORY: crm (Tier 1 — pre-built connectors)
- HubSpot, Salesforce, Pipedrive, GoHighLevel, ActiveCampaign
  (setup_cost=750, monthly_cost=150, api_available=true, connector_type='pre-built')

CATEGORY: crm (Tier 2 — custom REST mapping)
- Zoho CRM, Monday Sales CRM, Freshsales, Close, Keap
  (setup_cost=1500, monthly_cost=250, api_available=true, connector_type='custom-rest')

CATEGORY: booking (Tier 1)
- Calendly, Google Calendar
  (setup_cost=750, monthly_cost=150, api_available=true, connector_type='pre-built')

CATEGORY: booking (Tier 2)
- Acuity Scheduling, Outlook Calendar, SimplyBook.me
  (setup_cost=1500, monthly_cost=250, api_available=true, connector_type='custom-rest')

CATEGORY: booking (Tier 3 — legacy/limited API)
- ServiceTitan, Mindbody, Vagaro, Jobber, Housecall Pro
  (setup_cost=4000, monthly_cost=500, api_available=false, connector_type='legacy',
   notes='Requires custom integration or limited API access')

CATEGORY: email (Tier 1)
- Mailchimp, Klaviyo, ConvertKit
  (setup_cost=750, monthly_cost=150, api_available=true, connector_type='pre-built')

CATEGORY: email (Tier 2)
- Brevo, Drip, Beehiiv, ActiveCampaign (email-only)
  (setup_cost=1500, monthly_cost=250, api_available=true, connector_type='custom-rest')

CATEGORY: payment (Tier 1)
- Stripe, Square, PayPal
  (setup_cost=750, monthly_cost=150, api_available=true, connector_type='pre-built')

CATEGORY: payment (Tier 2)
- Adyen, Authorize.net, QuickBooks Payments
  (setup_cost=1500, monthly_cost=250, api_available=true, connector_type='custom-rest')

CATEGORY: helpdesk (Tier 1)
- Zendesk, Intercom, HubSpot Service
  (setup_cost=750, monthly_cost=150, api_available=true, connector_type='pre-built')

CATEGORY: helpdesk (Tier 2)
- Freshdesk
  (setup_cost=1500, monthly_cost=250, api_available=true, connector_type='custom-rest')

CATEGORY: accounting (Tier 1)
- QuickBooks Online, Xero
  (setup_cost=750, monthly_cost=150, api_available=true, connector_type='pre-built')

CATEGORY: accounting (Tier 2)
- FreshBooks, Wave
  (setup_cost=1500, monthly_cost=250, api_available=true, connector_type='custom-rest')

CATEGORY: erp (Tier 3 — no public API or legacy)
- SAP, NetSuite, Oracle ERP, Microsoft Dynamics
  (setup_cost=4000, monthly_cost=500, api_available=false, connector_type='no-api',
   notes='Enterprise ERP — requires custom build or middleware')

CATEGORY: platform (context — not billed as integrations, used for platform detection)
- Shopify, WooCommerce, WordPress, Webflow, Wix, Squarespace, Magento, Next.js Custom
  (tier=1 for headless/API-capable, tier=3 for closed platforms)
  - Shopify: tier=2 (limited API for agentic protocols), setup_cost=1500, monthly_cost=250
  - WooCommerce: tier=2, connector_type='custom-rest'
  - WordPress: tier=2, connector_type='custom-rest'
  - Webflow: tier=1, connector_type='pre-built'
  - Wix: tier=3, api_available=false, connector_type='no-api', notes='Closed platform — limited protocol compliance'
  - Squarespace: tier=3, api_available=false, connector_type='no-api', notes='Closed platform'
  - Magento: tier=3, connector_type='legacy', notes='Requires enterprise integration'
  - Next.js Custom: tier=1, connector_type='pre-built', notes='Full headless — native protocol support'

Total: minimum 40 rows. Use multi-row INSERT syntax for efficiency:
```sql
INSERT INTO integrations_catalog (name, category, tier, setup_cost, monthly_cost, api_available, connector_type, notes)
VALUES
  ('HubSpot', 'crm', 1, 750.00, 150.00, true, 'pre-built', NULL),
  ('Salesforce', 'crm', 1, 750.00, 150.00, true, 'pre-built', NULL),
  ...
ON CONFLICT (name) DO NOTHING;
```
  </action>
  <verify>
    <automated>grep -c "ON CONFLICT (name) DO NOTHING" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/011_seed_integrations_catalog.sql && grep -c "HubSpot" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/011_seed_integrations_catalog.sql && grep -c "NetSuite" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/011_seed_integrations_catalog.sql && grep -c "ServiceTitan" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/011_seed_integrations_catalog.sql</automated>
  </verify>
  <done>File exists with ON CONFLICT (name) DO NOTHING, includes HubSpot (Tier 1), Zoho CRM (Tier 2), NetSuite (Tier 3), ServiceTitan (Tier 3, booking), and at least 40 rows total across all categories.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 012 — packages seed (6 packages)</name>
  <files>
    supabase/migrations/012_seed_packages.sql
  </files>
  <action>
Write supabase/migrations/012_seed_packages.sql using INSERT INTO packages ... ON CONFLICT (slug) DO NOTHING to make it idempotent.

Seed exactly 6 packages with these LOCKED values (from 02-CONTEXT.md):

1. Bronze
   - name='Bronze', slug='bronze', base_setup=16000, base_monthly=3500
   - tier1_slots=3, tier2_slots=0, tier3_slots=0, is_legacy_only=false
   - features_json: JSON array with these strings:
     ["Up to 3 Tier 1 integrations", "Agentic commerce protocol compliance", "Basic chatbot (web only)", "Weekly content calendar", "Monthly authority map", "Email + chat support", "Proposal generation", "CRM connection (1)"]

2. Silver
   - name='Silver', slug='silver', base_setup=28000, base_monthly=6500
   - tier1_slots=6, tier2_slots=1, tier3_slots=0, is_legacy_only=false
   - features_json:
     ["Up to 6 Tier 1 integrations + 1 Tier 2", "Full protocol stack (UCP + ACP + AP2)", "Chatbot with SMS channel (Twilio/Vonage)", "Bi-weekly blog production", "Monthly press release", "Monthly authority map", "Priority support", "Proposal generation", "CRM connection (up to 2)", "48-hour follow-up automation"]

3. Gold
   - name='Gold', slug='gold', base_setup=48000, base_monthly=12000
   - tier1_slots=12, tier2_slots=3, tier3_slots=1, is_legacy_only=false
   - features_json:
     ["Up to 12 Tier 1 + 3 Tier 2 + 1 Tier 3 integrations", "Full protocol stack", "Multi-channel chatbot (Web + SMS + Voice + WhatsApp)", "Weekly blog production pipeline", "Weekly press releases", "Monthly authority map", "Dedicated account manager", "Proposal generation", "CRM connections (up to 5)", "48-hour follow-up + call scheduling automation", "Remotion video sidecars for all content"]

4. Core
   - name='Core', slug='core', base_setup=75000, base_monthly=0
   - tier1_slots=99, tier2_slots=99, tier3_slots=99, is_legacy_only=false
   - features_json:
     ["Unlimited integrations (all tiers)", "Full protocol stack", "Multi-channel chatbot — all channels", "Full content production pipeline", "Custom monthly retainer (negotiated separately)", "White-glove onboarding", "Dedicated engineering resources", "Custom SLA", "All CRM adapters included", "Executive reporting dashboard"]

5. Shopify Starter
   - name='Shopify Starter', slug='shopify-starter', base_setup=8500, base_monthly=2000
   - tier1_slots=2, tier2_slots=0, tier3_slots=0, is_legacy_only=true
   - features_json:
     ["Up to 2 Tier 1 integrations", "Shopify-compatible protocol layer", "Basic chatbot (web only)", "Monthly content calendar", "Legacy platform optimizations", "Email support"]

6. Shopify Growth
   - name='Shopify Growth', slug='shopify-growth', base_setup=16000, base_monthly=4000
   - tier1_slots=4, tier2_slots=1, tier3_slots=0, is_legacy_only=true
   - features_json:
     ["Up to 4 Tier 1 integrations + 1 Tier 2", "Shopify-compatible protocol layer", "Chatbot with SMS channel", "Bi-weekly content calendar", "Monthly authority map", "Priority support", "CRM connection (1)"]

Use multi-row INSERT with ON CONFLICT (slug) DO NOTHING. The features_json value must be valid JSONB — use single quotes with proper escaping or use $$ dollar quoting for the JSON strings.

NOTE: For JSONB literal insertion in PostgreSQL, cast the JSON array string explicitly:
```sql
INSERT INTO packages (name, slug, base_setup, base_monthly, tier1_slots, tier2_slots, tier3_slots, features_json, is_legacy_only)
VALUES
  ('Bronze', 'bronze', 16000.00, 3500.00, 3, 0, 0,
   '["Up to 3 Tier 1 integrations", "Agentic commerce protocol compliance"]'::jsonb,
   false),
  ...
ON CONFLICT (slug) DO NOTHING;
```
  </action>
  <verify>
    <automated>grep -c "ON CONFLICT (slug) DO NOTHING" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/012_seed_packages.sql && grep -c "shopify-starter" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/012_seed_packages.sql && grep -c "core" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/012_seed_packages.sql</automated>
  </verify>
  <done>File exists with ON CONFLICT (slug) DO NOTHING, all 6 package slugs present (bronze, silver, gold, core, shopify-starter, shopify-growth), features_json cast to ::jsonb, is_legacy_only correct (true only for shopify-* packages).</done>
</task>

</tasks>

<verification>
Both seed files exist in supabase/migrations/ and are idempotent.
Run: ls C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/ — shows 012 files.
Run: grep "ON CONFLICT" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/011_seed_integrations_catalog.sql — returns match.
Run: grep -c "shopify-growth" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/012_seed_packages.sql — returns 1.
</verification>

<success_criteria>
- 011_seed_integrations_catalog.sql: 40+ tool rows, all 8 categories covered, correct tier/cost mapping, ON CONFLICT idempotent
- 012_seed_packages.sql: exactly 6 rows with LOCKED slot counts and pricing, features_json as valid jsonb arrays, is_legacy_only=true only for shopify-* rows
- Both files can be re-run without duplicating data
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-schema-data-architecture/02-03-SUMMARY.md`
</output>
