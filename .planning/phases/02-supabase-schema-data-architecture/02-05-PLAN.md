---
phase: 02-supabase-schema-data-architecture
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified: []
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07]

must_haves:
  truths:
    - "All 7 new tables exist in Supabase (integrations_catalog, packages, proposals, blog_posts, authority_maps, chatbot_sessions, press_releases)"
    - "RLS is enabled on all 7 new tables — a SELECT with anon key returns 0 rows or permission denied"
    - "integrations_catalog contains 40+ rows including HubSpot (tier=1), Zoho CRM (tier=2), NetSuite (tier=3)"
    - "packages contains exactly 6 rows including core (tier1_slots=99) and shopify-starter (is_legacy_only=true)"
    - "blog_posts.authority_map_id FK constraint fk_blog_posts_authority_map exists"
  artifacts:
    - path: "supabase/migrations/004_integrations_catalog.sql"
      provides: "Applied to Supabase — table exists in DB"
      contains: "CREATE TABLE IF NOT EXISTS integrations_catalog"
    - path: "supabase/migrations/012_seed_packages.sql"
      provides: "Applied to Supabase — 6 package rows exist in DB"
      contains: "INSERT INTO packages"
  key_links:
    - from: "Supabase DB (cdifobufvgfpbcbvicjs)"
      to: "Phase 3 pricing engine"
      via: "lib/integrations/catalog.ts will query integrations_catalog by name"
      pattern: "SELECT * FROM integrations_catalog WHERE name = $1"
    - from: "Supabase DB (cdifobufvgfpbcbvicjs)"
      to: "Phase 4 intake agent"
      via: "Proposals table receives INSERT from generate-proposal edge function"
      pattern: "INSERT INTO proposals"
---

<objective>
Apply all 9 migration files (004–012) to the live Supabase project using the Supabase CLI. Verify that all 7 tables exist, RLS is active on each, seed data is present, and the FK constraint from blog_posts to authority_maps is in place. This plan has a human-verify checkpoint because it touches the live production database.

Purpose: Without running the migrations, the SQL files are inert. This plan is the deployment step that activates Phase 2's work in the actual Supabase project.

Output: 7 live tables in Supabase project cdifobufvgfpbcbvicjs, 40+ seeded integrations, 6 seeded packages.
</objective>

<execution_context>
@C:/Users/Bev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-supabase-schema-data-architecture/02-CONTEXT.md

<interfaces>
<!-- Supabase project details -->
Project ref: cdifobufvgfpbcbvicjs
Region: us-east-1
URL: https://cdifobufvgfpbcbvicjs.supabase.co
Access token: sbp_9763ae3189e29a7f5180816da338c4058e192d2a

<!-- Migration files to apply (in order) -->
004_integrations_catalog.sql  -- table + RLS
005_packages.sql               -- table + RLS
006_proposals.sql              -- table + RLS
007_blog_posts.sql             -- table + RLS (no FK yet)
008_authority_maps.sql         -- table + RLS
009_chatbot_sessions.sql       -- table + RLS
010_press_releases.sql         -- table + RLS + FK patch for blog_posts
011_seed_integrations_catalog.sql  -- 40+ rows
012_seed_packages.sql          -- 6 rows

<!-- Supabase CLI commands -->
-- Link project (if not already linked):
npx supabase link --project-ref cdifobufvgfpbcbvicjs

-- Push all pending migrations:
npx supabase db push

-- Or apply individual migration via psql (fallback):
npx supabase db execute --file supabase/migrations/004_integrations_catalog.sql

-- Check tables via Supabase CLI:
npx supabase db query "SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' ORDER BY table_name"

-- Check row counts:
npx supabase db query "SELECT COUNT(*) FROM integrations_catalog"
npx supabase db query "SELECT COUNT(*) FROM packages"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link Supabase project and push all migrations</name>
  <files></files>
  <action>
From the project root C:/Users/Bev/Downloads/ASCW/adamsilva, run:

STEP 1 — Check if Supabase CLI is available and project is linked:
  npx supabase --version
  cat supabase/config.toml 2>/dev/null || echo "No config.toml"

STEP 2 — If not linked, link the project:
  npx supabase link --project-ref cdifobufvgfpbcbvicjs
  (When prompted for password: use DB password from MEMORY.md = ASCv2Secure2026)

STEP 3 — Push all pending migrations (004–012):
  npx supabase db push

  If db push requires confirmation, pass --include-all flag:
  npx supabase db push --include-all

STEP 4 — Verify tables exist:
  npx supabase db query "SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' AND table_name IN ('integrations_catalog','packages','proposals','blog_posts','authority_maps','chatbot_sessions','press_releases') ORDER BY table_name"

  Expected: 7 rows returned

STEP 5 — Verify seed data:
  npx supabase db query "SELECT COUNT(*) as total FROM integrations_catalog"
  npx supabase db query "SELECT COUNT(*) as total FROM packages"
  npx supabase db query "SELECT slug, tier1_slots, tier2_slots, tier3_slots, is_legacy_only FROM packages ORDER BY slug"

FALLBACK — If npx supabase db push fails (e.g. migration tracking conflict):
  Apply migrations individually using psql or the Supabase dashboard SQL editor.
  Order matters: apply 004→005→006→007→008→009→010→011→012 in sequence.
  If using dashboard: go to https://supabase.com/dashboard/project/cdifobufvgfpbcbvicjs/database/query
  and paste each migration file's contents and run.

NOTE: Do NOT run migrations 001, 002, or 003 — they already exist in the database. If supabase db push tries to replay them, use --include-all to handle conflicts gracefully (IF NOT EXISTS clauses protect against duplication).
  </action>
  <verify>
    <automated>cd C:/Users/Bev/Downloads/ASCW/adamsilva &amp;&amp; npx supabase db query "SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' AND table_name IN ('integrations_catalog','packages','proposals','blog_posts','authority_maps','chatbot_sessions','press_releases') ORDER BY table_name" 2>&amp;1</automated>
  </verify>
  <done>All 7 table names returned from the query. No error output. Supabase CLI reports migrations applied successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Verify RLS, seed counts, and FK constraint</name>
  <files></files>
  <action>
Run these verification queries against the live Supabase database:

STEP 1 — Verify RLS is enabled on all 7 new tables:
  npx supabase db query "SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public' AND tablename IN ('integrations_catalog','packages','proposals','blog_posts','authority_maps','chatbot_sessions','press_releases') ORDER BY tablename"
  Expected: rowsecurity = true for all 7 rows

STEP 2 — Verify seed counts:
  npx supabase db query "SELECT COUNT(*) as integrations_count FROM integrations_catalog"
  Expected: 40 or more

  npx supabase db query "SELECT slug, tier1_slots, is_legacy_only FROM packages ORDER BY base_setup"
  Expected: 6 rows — shopify-starter (t1=2, legacy=true), shopify-growth (t1=4, legacy=true), bronze (t1=3, legacy=false), silver (t1=6, legacy=false), gold (t1=12, legacy=false), core (t1=99, legacy=false)

STEP 3 — Verify FK constraint exists:
  npx supabase db query "SELECT constraint_name FROM information_schema.table_constraints WHERE table_name = 'blog_posts' AND constraint_type = 'FOREIGN KEY'"
  Expected: fk_blog_posts_authority_map returned

STEP 4 — Spot-check integration tiers:
  npx supabase db query "SELECT name, tier, setup_cost FROM integrations_catalog WHERE name IN ('HubSpot', 'Zoho CRM', 'NetSuite') ORDER BY tier"
  Expected:
    HubSpot  | 1 | 750.00
    Zoho CRM | 2 | 1500.00
    NetSuite | 3 | 4000.00

If any check fails:
  - Missing table: re-apply that specific migration file via dashboard SQL editor
  - RLS not enabled: run ALTER TABLE [table] ENABLE ROW LEVEL SECURITY; manually
  - Wrong seed data: re-run the relevant seed migration (ON CONFLICT DO NOTHING makes re-runs safe)
  - FK missing: run the DO block from migration 010 manually in the dashboard SQL editor
  </action>
  <verify>
    <automated>cd C:/Users/Bev/Downloads/ASCW/adamsilva &amp;&amp; npx supabase db query "SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public' AND tablename IN ('integrations_catalog','packages','proposals','blog_posts','authority_maps','chatbot_sessions','press_releases') ORDER BY tablename" 2>&amp;1</automated>
  </verify>
  <done>All 7 tables show rowsecurity=true. integrations_catalog has 40+ rows. packages has 6 rows with correct slot counts. FK constraint fk_blog_posts_authority_map exists on blog_posts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Applied 9 SQL migration files (004–012) to Supabase project cdifobufvgfpbcbvicjs:
  - 7 new tables: integrations_catalog, packages, proposals, blog_posts, authority_maps, chatbot_sessions, press_releases
  - RLS enabled on all 7 tables
  - 40+ integration tools seeded (Tier 1/2/3 across all categories)
  - 6 package definitions seeded (Bronze, Silver, Gold, Core, Shopify Starter, Shopify Growth)
  - FK constraint from blog_posts.authority_map_id to authority_maps.id
  </what-built>
  <how-to-verify>
  1. Go to: https://supabase.com/dashboard/project/cdifobufvgfpbcbvicjs/database/tables
  2. Confirm all 7 new tables appear in the table list alongside the existing 3 (contact_submissions, leads, newsletter_signups)
  3. Click "integrations_catalog" → check that rows exist, tier column shows 1/2/3 values, name="HubSpot" is present
  4. Click "packages" → check that 6 rows exist, slug="core" has tier1_slots=99
  5. Go to: https://supabase.com/dashboard/project/cdifobufvgfpbcbvicjs/database/policies
  6. Confirm each of the 7 new tables shows "RLS enabled" and has a "service_role_all" policy
  </how-to-verify>
  <resume-signal>Type "verified" once tables, seed data, and RLS policies are confirmed in the Supabase dashboard. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Live Supabase database contains all 7 new tables with RLS.
Run: npx supabase db query "SELECT COUNT(*) FROM integrations_catalog" — returns 40+
Run: npx supabase db query "SELECT COUNT(*) FROM packages" — returns 6
Run: npx supabase db query "SELECT rowsecurity FROM pg_tables WHERE tablename='proposals'" — returns true
</verification>

<success_criteria>
- All 9 migrations (004–012) applied without errors
- 7 new tables exist in public schema
- RLS enabled (rowsecurity=true) on all 7 new tables
- integrations_catalog: 40+ rows, HubSpot=tier1, Zoho CRM=tier2, NetSuite=tier3
- packages: 6 rows, core has tier1_slots=99, shopify-* rows have is_legacy_only=true
- FK constraint fk_blog_posts_authority_map exists on blog_posts
- Human confirms via Supabase dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-schema-data-architecture/02-05-SUMMARY.md`
</output>
