---
phase: 02-supabase-schema-data-architecture
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/generate-proposal/index.ts
  - supabase/functions/send-proposal-email/index.ts
  - supabase/functions/create-crm-deal/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "generate-proposal edge function scaffold exists and returns 200 with JSON body"
    - "send-proposal-email edge function scaffold exists and returns 200 with JSON body"
    - "create-crm-deal edge function scaffold exists and returns 200 with JSON body"
    - "All three functions follow the Deno TypeScript pattern from existing supabase/functions/"
  artifacts:
    - path: "supabase/functions/generate-proposal/index.ts"
      provides: "HTTP scaffold returning {success: true, message: 'generate-proposal scaffold'}"
      contains: "serve"
    - path: "supabase/functions/send-proposal-email/index.ts"
      provides: "HTTP scaffold returning {success: true, message: 'send-proposal-email scaffold'}"
      contains: "serve"
    - path: "supabase/functions/create-crm-deal/index.ts"
      provides: "HTTP scaffold returning {success: true, message: 'create-crm-deal scaffold'}"
      contains: "serve"
  key_links:
    - from: "supabase/functions/generate-proposal/index.ts"
      to: "Phase 4 implementation"
      via: "Full implementation in Phase 4 — this scaffold only establishes the function name and deploy target"
      pattern: "generate-proposal scaffold"
---

<objective>
Write three Deno TypeScript edge function scaffold files: generate-proposal, send-proposal-email, and create-crm-deal. These are stubs that deploy without errors and return 200 responses — full implementation is Phase 4.

Purpose: The Supabase edge function infrastructure must be established and deployed in Phase 2 so that Phase 4 can implement the function bodies against already-existing deploy targets. Avoids cold-start configuration work during the intake agent build.

Output: 3 Deno TypeScript index.ts files in supabase/functions/[name]/ — following the exact pattern of the existing email-service function.
</objective>

<execution_context>
@C:/Users/Bev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-supabase-schema-data-architecture/02-CONTEXT.md

<interfaces>
<!-- Existing edge function pattern from supabase/functions/email-service/index.ts -->
```typescript
import { serve } from 'https://deno.land/std@0.208.0/http/server.ts'

// Function-specific env vars go here
// const RESEND_API_KEY = Deno.env.get('RESEND_API_KEY') ?? ''

serve(async (req) => {
  // Parse request body
  // const body = await req.json()

  return new Response(
    JSON.stringify({ success: true, message: 'function-name scaffold' }),
    { headers: { 'Content-Type': 'application/json' }, status: 200 }
  )
})
```

<!-- Supabase project -->
Project ref: cdifobufvgfpbcbvicjs
Existing functions: email-service, generate-guide, team-notifications
New functions: generate-proposal, send-proposal-email, create-crm-deal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write generate-proposal and send-proposal-email scaffolds</name>
  <files>
    supabase/functions/generate-proposal/index.ts
    supabase/functions/send-proposal-email/index.ts
  </files>
  <action>
Write supabase/functions/generate-proposal/index.ts:
- Match the Deno import pattern from existing functions (use std@0.208.0 to match email-service)
- Import: import { serve } from 'https://deno.land/std@0.208.0/http/server.ts'
- Add a comment block at the top describing what Phase 4 will implement:
  // Phase 4 Implementation:
  // - Accept: { prospect_name, prospect_email, industry, platform, integrations_detected[], goals[], recommended_package }
  // - Generate proposal record in Supabase proposals table
  // - Return: { success: true, proposal_id, setup_total, monthly_total }
- serve() handler:
  - If req.method === 'OPTIONS': return CORS preflight (status 204, no body)
  - All other methods: return JSON body { success: true, message: 'generate-proposal scaffold', phase: 4 } with status 200
- Include CORS headers on all responses:
  'Access-Control-Allow-Origin': '*'
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type'
  'Content-Type': 'application/json'

Write supabase/functions/send-proposal-email/index.ts:
- Same Deno import, same CORS handling, same OPTIONS preflight response
- Comment block:
  // Phase 4 Implementation:
  // - Accept: { proposal_id, prospect_email, prospect_name, proposal_pdf_url }
  // - Send branded proposal email via Resend with PDF attachment link
  // - Return: { success: true, email_id }
- serve() handler: return JSON { success: true, message: 'send-proposal-email scaffold', phase: 4 } with status 200
  </action>
  <verify>
    <automated>ls C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/generate-proposal/index.ts &amp;&amp; ls C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/send-proposal-email/index.ts &amp;&amp; grep -l "generate-proposal scaffold" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/generate-proposal/index.ts &amp;&amp; grep -l "send-proposal-email scaffold" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/send-proposal-email/index.ts</automated>
  </verify>
  <done>Both files exist with correct Deno imports, CORS preflight handling, and scaffold JSON responses. Function names match directory names exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Write create-crm-deal scaffold</name>
  <files>
    supabase/functions/create-crm-deal/index.ts
  </files>
  <action>
Write supabase/functions/create-crm-deal/index.ts:
- Same Deno import, same CORS handling, same OPTIONS preflight response
- Comment block:
  // Phase 4 Implementation:
  // - Accept: { prospect_name, prospect_email, company, proposal_id, recommended_package, setup_total, monthly_total, crm_provider }
  // - Create contact + deal in configured CRM via webhook or direct API adapter
  // - CRM_WEBHOOK_URL env var: Deno.env.get('CRM_WEBHOOK_URL')
  // - Return: { success: true, crm_deal_id, crm_contact_id }
- Note env var placeholder (commented out, not active):
  // const CRM_WEBHOOK_URL = Deno.env.get('CRM_WEBHOOK_URL') ?? ''
- serve() handler: return JSON { success: true, message: 'create-crm-deal scaffold', phase: 4 } with status 200
- Same CORS headers as other scaffolds

After writing all 3 scaffold files, verify the directory structure:
  supabase/functions/
    email-service/index.ts        (existing)
    generate-guide/index.ts       (existing)
    team-notifications/index.ts   (existing)
    generate-proposal/index.ts    (NEW)
    send-proposal-email/index.ts  (NEW)
    create-crm-deal/index.ts      (NEW)
  </action>
  <verify>
    <automated>ls C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/create-crm-deal/index.ts &amp;&amp; grep -l "create-crm-deal scaffold" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/create-crm-deal/index.ts &amp;&amp; ls C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/</automated>
  </verify>
  <done>create-crm-deal/index.ts exists with correct Deno imports, CORS handling, commented CRM_WEBHOOK_URL env var, and scaffold JSON response. All 6 function directories visible in supabase/functions/.</done>
</task>

</tasks>

<verification>
All 3 new edge function scaffold files exist.
Run: ls C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/ — shows 6 directories including generate-proposal, send-proposal-email, create-crm-deal.
Run: grep "scaffold" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/generate-proposal/index.ts — returns match.
Run: grep "deno.land/std@0.208.0" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/functions/create-crm-deal/index.ts — returns match.
</verification>

<success_criteria>
- All 3 scaffold files use deno.land/std@0.208.0 import (matches existing email-service)
- All 3 handle OPTIONS CORS preflight (status 204)
- All 3 return { success: true, message: '[function-name] scaffold', phase: 4 } on any non-OPTIONS request
- All 3 include CORS headers on responses
- Phase 4 implementation hints are in comment blocks
- No active logic that could fail — pure scaffolds
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-schema-data-architecture/02-04-SUMMARY.md`
</output>
