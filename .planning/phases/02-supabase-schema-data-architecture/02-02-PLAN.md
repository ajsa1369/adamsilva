---
phase: 02-supabase-schema-data-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/008_authority_maps.sql
  - supabase/migrations/009_chatbot_sessions.sql
  - supabase/migrations/010_press_releases.sql
autonomous: true
requirements: [DATA-04, DATA-05, DATA-06, DATA-07]

must_haves:
  truths:
    - "authority_maps table can store a monthly topical authority map per client with topics_json and approval timestamp"
    - "chatbot_sessions table can store full session history including messages (jsonb) and outcome per channel"
    - "press_releases table can store a draft + schema + wire service distribution record"
    - "Every new table has RLS enabled and denies all access without service-role key"
  artifacts:
    - path: "supabase/migrations/008_authority_maps.sql"
      provides: "authority_maps table + RLS"
      contains: "CREATE TABLE IF NOT EXISTS authority_maps"
    - path: "supabase/migrations/009_chatbot_sessions.sql"
      provides: "chatbot_sessions table + RLS"
      contains: "CREATE TABLE IF NOT EXISTS chatbot_sessions"
    - path: "supabase/migrations/010_press_releases.sql"
      provides: "press_releases table + RLS"
      contains: "CREATE TABLE IF NOT EXISTS press_releases"
  key_links:
    - from: "supabase/migrations/007_blog_posts.sql"
      to: "supabase/migrations/008_authority_maps.sql"
      via: "blog_posts.authority_map_id references authority_maps.id — FK added in 010b or via ALTER TABLE in this plan after authority_maps exists"
      pattern: "authority_map_id UUID REFERENCES authority_maps(id)"
---

<objective>
Write the final three SQL migration files for Phase 2: authority_maps, chatbot_sessions, and press_releases. Also adds the deferred FK constraint from blog_posts.authority_map_id to authority_maps.id (safe to do after 008 creates authority_maps).

Purpose: Completes the 7-table schema foundation. These tables serve Phase 5 (authority maps), Phase 8 (chatbot), and Phase 7 (press releases). Must exist before those phases begin.

Output: 3 SQL migration files in supabase/migrations/ ready to be applied via Supabase CLI.
</objective>

<execution_context>
@C:/Users/Bev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-supabase-schema-data-architecture/02-CONTEXT.md

<interfaces>
<!-- Existing migration pattern from supabase/migrations/001_contact_submissions.sql -->
```sql
CREATE TABLE IF NOT EXISTS table_name (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
  -- columns...
);
CREATE INDEX IF NOT EXISTS idx_name ON table(column);
ALTER TABLE table ENABLE ROW LEVEL SECURITY;
CREATE POLICY "service_role_all" ON table
  FOR ALL USING (auth.role() = 'service_role');
```

<!-- LOCKED column sets from 02-CONTEXT.md -->
-- authority_maps: id, client_id (text), month (date or text like '2026-03'),
--   topics_json (jsonb), created_at, approved_at (nullable timestamptz)
-- chatbot_sessions: id, client_id (text), channel (text: web/sms/voice/whatsapp),
--   session_start (timestamptz), messages (jsonb), outcome (text),
--   crm_contact_id (text), appointment_booked (boolean), lead_value_estimate (numeric)
-- press_releases: id, client_id (text), title (text), draft_text (text),
--   schema_json (jsonb), video_url (text), wire_service (text),
--   distributed_at (timestamptz), status (text)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 008 (authority_maps) and 009 (chatbot_sessions)</name>
  <files>
    supabase/migrations/008_authority_maps.sql
    supabase/migrations/009_chatbot_sessions.sql
  </files>
  <action>
Write supabase/migrations/008_authority_maps.sql:
- Table: authority_maps
- Columns:
  - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
  - created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
  - client_id TEXT NOT NULL — text identifier, no FK
  - month TEXT NOT NULL — stored as 'YYYY-MM' string (e.g. '2026-03') for easy querying
  - topics_json JSONB NOT NULL DEFAULT '[]'::jsonb — ranked topics with citation opportunity scores
  - approved_at TIMESTAMPTZ — null until client approves; set by approval webhook
- UNIQUE constraint: UNIQUE (client_id, month) — one authority map per client per month
- Indexes: idx_authority_maps_client_id (client_id), idx_authority_maps_month (month)
- RLS: Enable + service_role_all policy

Write supabase/migrations/009_chatbot_sessions.sql:
- Table: chatbot_sessions
- Columns:
  - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
  - created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
  - client_id TEXT NOT NULL — identifies which client's chatbot this session belongs to
  - channel TEXT NOT NULL DEFAULT 'web' CHECK (channel IN ('web', 'sms', 'voice', 'whatsapp'))
  - session_start TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - messages JSONB NOT NULL DEFAULT '[]'::jsonb — array of {role, content, timestamp}
  - outcome TEXT CHECK (outcome IN ('qualified', 'unqualified', 'escalated', 'abandoned', 'booked', 'converted'))
  - crm_contact_id TEXT — CRM contact ID after lead creation
  - appointment_booked BOOLEAN NOT NULL DEFAULT false
  - lead_value_estimate NUMERIC(10,2)
- Indexes: idx_chatbot_sessions_client_id (client_id), idx_chatbot_sessions_channel (channel), idx_chatbot_sessions_outcome (outcome), idx_chatbot_sessions_created_at (created_at DESC)
- RLS: Enable + service_role_all policy
  </action>
  <verify>
    <automated>grep -l "CREATE TABLE IF NOT EXISTS authority_maps" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/008_authority_maps.sql && grep -l "CREATE TABLE IF NOT EXISTS chatbot_sessions" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/009_chatbot_sessions.sql && grep -c "ENABLE ROW LEVEL SECURITY" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/008_authority_maps.sql && grep -c "ENABLE ROW LEVEL SECURITY" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/009_chatbot_sessions.sql</automated>
  </verify>
  <done>Both files exist with correct column definitions, CHECK constraints on channel and outcome, UNIQUE constraint on authority_maps(client_id, month), RLS enabled, and service_role_all policies.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 010 (press_releases) and 010b FK constraint patch</name>
  <files>
    supabase/migrations/010_press_releases.sql
  </files>
  <action>
Write supabase/migrations/010_press_releases.sql with TWO sections:

SECTION 1 — press_releases table:
- Table: press_releases
- Columns:
  - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
  - created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
  - client_id TEXT NOT NULL
  - title TEXT NOT NULL
  - draft_text TEXT NOT NULL
  - schema_json JSONB DEFAULT '{}'::jsonb
  - video_url TEXT
  - wire_service TEXT CHECK (wire_service IN ('business-wire', 'pr-newswire', 'ein-presswire', 'accesswire', 'none'))
  - distributed_at TIMESTAMPTZ — null until distribution is confirmed
  - status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'review', 'approved', 'distributed', 'archived'))
  - updated_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
- Indexes: idx_press_releases_client_id (client_id), idx_press_releases_status (status)
- RLS: Enable + service_role_all policy

SECTION 2 — FK patch for blog_posts.authority_map_id:
After the press_releases table is created, add a comment and the deferred FK:
```sql
-- Add FK from blog_posts to authority_maps (deferred until both tables exist)
-- This runs safely after migration 007 (blog_posts) and 008 (authority_maps)
ALTER TABLE blog_posts
  ADD CONSTRAINT IF NOT EXISTS fk_blog_posts_authority_map
  FOREIGN KEY (authority_map_id) REFERENCES authority_maps(id) ON DELETE SET NULL;
```

NOTE: PostgreSQL does not support "ADD CONSTRAINT IF NOT EXISTS" for FKs — use a DO block instead:
```sql
DO $$
BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM information_schema.table_constraints
    WHERE constraint_name = 'fk_blog_posts_authority_map'
    AND table_name = 'blog_posts'
  ) THEN
    ALTER TABLE blog_posts
      ADD CONSTRAINT fk_blog_posts_authority_map
      FOREIGN KEY (authority_map_id) REFERENCES authority_maps(id) ON DELETE SET NULL;
  END IF;
END $$;
```

This is idempotent and safe to run multiple times.
  </action>
  <verify>
    <automated>grep -l "CREATE TABLE IF NOT EXISTS press_releases" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/010_press_releases.sql && grep -l "fk_blog_posts_authority_map" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/010_press_releases.sql && grep -c "ENABLE ROW LEVEL SECURITY" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/010_press_releases.sql</automated>
  </verify>
  <done>010_press_releases.sql exists with correct columns, wire_service and status CHECK constraints, RLS, and the idempotent FK patch DO block for blog_posts.authority_map_id.</done>
</task>

</tasks>

<verification>
All 3 migration files (008–010) exist in supabase/migrations/.
Run: ls C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/ — should show 10 files (001–010).
Run: grep -c "ENABLE ROW LEVEL SECURITY" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/010_press_releases.sql — returns 1.
Run: grep "fk_blog_posts_authority_map" C:/Users/Bev/Downloads/ASCW/adamsilva/supabase/migrations/010_press_releases.sql — returns match.
</verification>

<success_criteria>
- 008_authority_maps.sql: UNIQUE(client_id, month), topics_json jsonb, approved_at nullable, RLS
- 009_chatbot_sessions.sql: channel CHECK in (web/sms/voice/whatsapp), messages jsonb, outcome CHECK, RLS
- 010_press_releases.sql: wire_service CHECK, status CHECK, draft_text text, RLS
- 010_press_releases.sql: contains idempotent DO block adding FK from blog_posts.authority_map_id → authority_maps.id
- All 3 files idempotent (CREATE TABLE IF NOT EXISTS)
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-schema-data-architecture/02-02-SUMMARY.md`
</output>
